# Live Status Support

This is the release contract for Atoll's bundled agent integrations. It was re-checked against each agent's primary documentation on 2026-07-19.

For the public-evidence comparison with Vibe Island, see [Competitive Lifecycle Compatibility](COMPETITIVE_COMPATIBILITY.md).

## State and delivery contract

- **Running**, **Needs Input**, **Needs Approval**, **Done**, **Failed**, and **Cancelled** are separate protocol states. A bundled adapter claims only states backed by a typed, documented provider event.
- Attention states are display-only. Atoll does not answer questions, approve tools, or change an agent's permission mode.
- Provider timestamps are retained as source metadata, but freshness and terminal dwell use Atoll's local receipt time. A future-skewed provider clock therefore cannot pin a session or reject later legitimate events.
- Active state expires after ten minutes without a newer event. Terminal rows remain in the registry for five seconds and receive a three-second UI dwell from the moment the app observes them, including delayed queued events.
- Terminal records remain as non-visible tombstones for ten minutes. This suppresses late cleanup events and duplicate completion notifications without hiding a genuinely newer turn.
- Repeated identical terminals do not extend their dwell. A later terminal correction does receive a new dwell, and a generic **Done** cleanup cannot overwrite a more specific **Failed** or **Cancelled** result.
- Socket delivery is at-least-once: a valid event is durably queued before the sender is acknowledged, and the queue item is removed only after the lifecycle registry is persisted. Duplicate replay is intentionally safe.

## Setup and readiness contract

Atoll changes agent configuration only after the user selects **Add Live Status**. Existing hooks are merged structurally, unrelated entries are retained, and malformed or explicitly disabled configurations are reported without being rewritten.

`Configured` means that Atoll verified its current user-level file, command bridge, and bridge permissions. It does **not** prove that a running agent has reloaded the file, that a trust prompt was accepted, or that enterprise/system policy permits user hooks. A plugin reload or new agent session may still be required.

Atoll honors inherited absolute or home-relative custom user homes for `CODEX_HOME`, `CLAUDE_CONFIG_DIR`, `COPILOT_HOME`, `GEMINI_CLI_HOME`, `QWEN_HOME`, `PI_CODING_AGENT_DIR`, `OPENCODE_CONFIG_DIR`, and `HERMES_HOME`. Relative custom homes are deliberately not guessed by the GUI because their meaning depends on the launching shell's working directory. Project, system, and managed-policy layers are never modified.

## Shipped integrations

| Agent | Boundary and states | Failure / cancellation fidelity | Input / approval fidelity | User config, version, and activation |
| --- | --- | --- | --- | --- |
| [Codex](https://developers.openai.com/codex/hooks) | Per turn: `UserPromptSubmit` -> Running; `Stop` -> Done. `Stop` is a response boundary that another hook can continue. | No typed failed or interrupted terminal is currently claimed. | None. `PermissionRequest` is a pre-decision policy hook, so treating every invocation as a visible user wait would create false positives. | `$CODEX_HOME/hooks.json`, default `~/.codex/hooks.json`. Atoll reports a user `config.toml` `[features] hooks = false` setting without changing it. New/changed command hooks require review through `/hooks`; managed policy can ignore user hooks. |
| [Claude Code](https://code.claude.com/docs/en/hooks) | Per response: `UserPromptSubmit` -> Running; `Stop` -> Done. `Stop` can continue the agent and does not fire on user interrupt. | `StopFailure` -> Failed for documented API errors; no reliable cancellation event is claimed. | Typed `permission_prompt` -> Needs Approval; `elicitation_dialog` and `agent_needs_input` -> Needs Input. Documented completion, denial, and post-tool events resume Running. | `$CLAUDE_CONFIG_DIR/settings.json`, default `~/.claude/settings.json`. `disableAllHooks` is preserved and reported. Existing sessions may need to reload settings. |
| [Gemini CLI](https://geminicli.com/docs/hooks/reference/) | Per agent turn: `BeforeAgent` -> Running; `AfterAgent` -> Done. An `AfterAgent` hook can request retry, so this is the observed response boundary rather than proof no continuation follows. | No typed failed/cancelled turn terminal is claimed. | `Notification` type `ToolPermission` -> Needs Approval; `AfterTool` resumes Running. | `$GEMINI_CLI_HOME/.gemini/settings.json`, default `~/.gemini/settings.json`. `hooksConfig.enabled = false` is preserved and reported. Runtime policy and reload state remain external. |
| [GitHub Copilot CLI](https://docs.github.com/en/copilot/reference/hooks-reference) | Per response: `userPromptSubmitted` -> Running; `agentStop` -> Done. `sessionEnd` is the full-session boundary. | `sessionEnd.reason`: `complete` -> Done; `error`/`timeout` -> Failed; `abort`/`user_exit` -> Cancelled. `errorOccurred` is deliberately ignored because it can be recoverable. | Typed `permission_prompt` -> Needs Approval; `elicitation_dialog` -> Needs Input. `postToolUse` and `postToolUseFailure` resume Running. | `$COPILOT_HOME/hooks/atoll.json`, default `~/.copilot/hooks/atoll.json`, schema version 1. `disableAllHooks` is preserved and reported. Repository and policy hooks can change effective behavior. |
| [Cursor Agent](https://cursor.com/docs/hooks) | Per turn: `beforeSubmitPrompt` -> Running; `stop` -> terminal. | `stop.status` preserves `completed` -> Done, `error` -> Failed, and `aborted` -> Cancelled. | No typed bundled attention mapping is claimed. | `~/.cursor/hooks.json`, schema version 1. Atoll verifies the user-level file only. |
| [Factory Droid](https://docs.factory.ai/reference/hooks-reference) | Per response: `UserPromptSubmit` -> Running; `Stop` -> Done. `Stop` can continue the agent and does not run on user interrupt. | No typed failed/cancelled turn terminal is claimed. | None. `Notification` carries free-form text rather than a stable notification type, so Atoll does not infer approval versus input from wording. | `~/.factory/hooks.json`. Droid may require `/hooks` review; `allowManagedHooksOnly` organization policy can ignore user hooks and cannot be verified from this file. |
| [Qoder](https://docs.qoder.com/en/cli/hooks) | Per response: `UserPromptSubmit` -> Running; `Stop` -> Done. `Stop` can continue the agent. | `StopFailure` -> Failed; no typed cancellation terminal is claimed. | `permission_prompt` -> Needs Approval; `elicitation_dialog` -> Needs Input. Elicitation completion/response, post-tool success/failure, and permission denial resume Running. | `~/.qoder/settings.json`. Changes take effect immediately according to the CLI docs; disabled or malformed user settings are preserved. |
| [Qwen Code](https://qwenlm.github.io/qwen-code-docs/en/users/features/hooks/) | Per response: `UserPromptSubmit` -> Running; `Stop` -> Done. `Stop` can continue the agent. | `StopFailure` -> Failed for documented API errors; no typed cancellation terminal is claimed. | Typed `permission_prompt` -> Needs Approval. Post-tool success/failure resumes Running. The documented but unimplemented `elicitation_dialog` type is not claimed. | `$QWEN_HOME/settings.json`, default `~/.qwen/settings.json`. `disableAllHooks` is preserved and reported. An inherited relative `QWEN_HOME` is not guessed. |
| [Pi](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/docs/extensions.md) | Per agent turn: `agent_start` -> Running; `agent_settled` -> Done. | `agent_settled` does not document distinct failed/cancelled outcomes, so Atoll does not infer them. | No typed bundled attention mapping is claimed. | `$PI_CODING_AGENT_DIR/extensions/atoll.ts`, default `~/.pi/agent/extensions/atoll.ts`; Pi 0.80.4 or newer is required because that release introduced the settled event with lifecycle context. Managed file content must match exactly. |
| [OpenCode](https://opencode.ai/docs/plugins/) | Per session activity cycle: `session.status` `busy`/`retry` -> Running; `session.idle` or idle status -> Done. | `session.error` -> Failed, except `MessageAbortedError` -> Cancelled. A following idle event cannot flatten or duplicate that result. | Current `permission.asked` and legacy `permission.updated` -> Needs Approval; `question.asked` -> Needs Input. Current and legacy reply/reject shapes resume Running. | `$OPENCODE_CONFIG_DIR/plugins/atoll.js`, default `~/.config/opencode/plugins/atoll.js`, loaded at startup. Current managed source must match; a restart/new session may be needed after repair. |
| [Hermes](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/hooks.md) | Per turn: `pre_llm_call` -> Running; `on_session_end` fires after every `run_conversation()` call and maps `completed` -> Done, `interrupted` -> Cancelled, otherwise Failed. | Distinct completed, interrupted, and other failure outcomes are retained. | Prompted `pre_approval_request` surfaces -> Needs Approval; smart auto-decisions are ignored. `post_approval_response` resumes Running. | Managed plugin under `$HERMES_HOME/plugins/atoll-live-status/`, default `~/.hermes/plugins/atoll-live-status/`. An inherited `HERMES_HOME` targets that active profile boundary; otherwise Atoll configures the default home and each valid named profile. Each plugin must be enabled; the Hermes command is bounded and setup reports failures rather than hanging. |
| [Amp](https://ampcode.com/manual/plugin-api) | Per agent turn: `agent.start` -> Running; `agent.end` -> terminal. | `agent.end.status` preserves `done` -> Done, `error` -> Failed, and `cancelled` -> Cancelled. The active thread `error` state is not treated as terminal. | Stable `thread.state` `awaiting-approval` -> Needs Approval; `running` resumes Running. Duplicate observable states are suppressed and subscriptions are released at `agent.end`. | `~/.config/amp/plugins/atoll.ts`. Run `plugins: reload` after changes; `amp -x` needs Amp's `--plugin-ready-timeout` for lifecycle-dependent plugins. |

## Deliberate exclusions

- **Kiro CLI** is not shipped because stable v2 and early-access v3 use incompatible hook schemas, the audited adapter did not cover the default agent and project-local agents consistently, and the stop hook is not an unconditional settled boundary.
- **Kimi Code** is not shipped because prompt/stop hooks can be blocked or continued, interruption needs a separate terminal event, and automatic setup could not safely validate arbitrary existing TOML.
- **CodeBuddy** is not shipped because CLI hooks are beta and version-gated, while its blockable stop contract and missing general cancellation event do not meet the stable release bar.

Atoll can add an agent when its stable, public event contract and executable fixtures support a conservative integration. A broad but ambiguous adapter is not counted as native lifecycle support.
